dist: trusty
sudo: false

language: node_js
node_js:
  - "8"

cache:
  directories:
     - ./node_modules

before_install:
  - npm install -g yarn --cache-min 999999999

install:
  - yarn

script:
  - yarn build
